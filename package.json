{
  "name": "banksystem",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start:dev": "cd backend && nest start --watch",
    "dev": "cd frontend && npm run dev",
    "start": "concurrently  \" npm run start:dev\" \"npm run dev\" ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Artem Dmysh",
  "license": "ISC",
  "dependencies": {
    "@fortawesome/react-fontawesome": "^3.1.1",
    "concurrently": "^9.2.1"
  }
}
